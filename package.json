{
  "name": "storage-facility-root",
  "version": "1.0.0",
  "private": true,
  "description": "Storage Facility Management SaaS Platform Root Runner",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "install": "npm --prefix backend install && npm --prefix frontend install --legacy-peer-deps",
    "install:frontend": "npm --prefix frontend install --legacy-peer-deps",
    "install:backend": "npm --prefix backend install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
